---
status: complete
---

# Implementation Plan: Design Crit

## Phases

- [x] Phase 1: Shared standards and phase sub-agent prompt — Create `design_review_standards.md` and `design_crit_phase_prompt.md`. These are the foundation that all phase sub-agents reference.
- [x] Phase 2: Reusable phase resource files — Create all seven `design_crit/phase_*.md` files. Independent of each other, depend on shared standards existing.
- [ ] Phase 3: Manager command reference and SKILL.md — Create `cmd_design_crit.md` (the main command reference) and update `SKILL.md` with the command entry. Depends on all previous files existing so cross-references are valid.
